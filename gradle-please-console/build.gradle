version "1.0"

dependencies {
    compile "org.json:json:20180130"
}

jar {
    manifest {
        attributes "Main-Class": "xyz.moonrabbit.gradlepleaseconsole.Launcher"
    }

    from configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
}